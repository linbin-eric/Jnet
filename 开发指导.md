# 背景

当前 WriteProcessor 体系是一个线程，AbstractWriteCompleteHandler是一个线程，想要实现一种只有一个线程的模型。

# 目标

1. WriteProcessor 不在运行在单独的线程里，对接口进行改造，接口cc.jfire.jnet.common.api.WriteProcessor.write增加一个入参类型是 IoBuffer 的 Consumer。当产生 buffer 的时候